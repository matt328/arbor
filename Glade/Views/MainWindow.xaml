<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Glade.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="using:CommunityToolkit.WinUI.Controls"
        xmlns:views="using:Glade.Views"
        xmlns:gladecontrols="using:Glade.Controls"
        xmlns:local="using:Glade"
        Title="Glade">

  <Window.SystemBackdrop>
    <MicaBackdrop />
  </Window.SystemBackdrop>

  <Grid>

    <Grid.RowDefinitions>
      <RowDefinition x:Name="TitleBar"
                     Height="Auto" />
      <RowDefinition x:Name="MainContentArea"
                     Height="*" />
      <RowDefinition x:Name="HorizontalSplitter"
                     Height="Auto" />
      <RowDefinition x:Name="LogViewer"
                     Height="200"
                     MinHeight="200" />
    </Grid.RowDefinitions>

    <gladecontrols:Titlebar x:Name="AppTitleBar"
                            Grid.Row="0" />

    <!-- Main Content Area -->
    <Grid Grid.Row="1">

      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="304"
                          MinWidth="304" />
        <ColumnDefinition Width="12" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <gladecontrols:SidebarView Grid.Column="0" />

      <controls:GridSplitter Grid.Column="1"
                             Width="12"
                             HorizontalAlignment="Left"
                             ResizeBehavior="PreviousAndNext"
                             ResizeDirection="Columns" />

      <Grid Grid.Column="2"
            Background="CornflowerBlue" />

    </Grid>

    <controls:GridSplitter Grid.Row="2"
                           Height="12"
                           VerticalAlignment="Top"
                           ResizeBehavior="PreviousAndNext"
                           ResizeDirection="Rows" />

    <gladecontrols:LogViewer Grid.Row="3"
                             VerticalAlignment="Stretch" />

  </Grid>
</Window>
  